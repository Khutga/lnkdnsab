name: cv_analyzer
description: LinkedIn İlan Analiz & CV Optimize - Hepsi Bir Arada

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  html: ^0.15.5
  file_picker: ^8.1.7
  shared_preferences: ^2.3.4
  url_launcher: ^6.3.1
  path_provider: ^2.1.5
  syncfusion_flutter_pdf: ^33.1.47

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
